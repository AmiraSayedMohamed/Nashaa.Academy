# Nashaa Website — layout-guardian-app

This is the frontend for the Nashaa marketing site (Vite + React + Tailwind).

Quick start (development):

```powershell
cd "d:\projects\Nashaa Website\v3-them1\layout-guardian-app"
npm install
npm run dev
```

Build for production:

```powershell

# Nashaa Website — layout-guardian-app

This is the frontend for the Nashaa marketing site (Vite + React + Tailwind).
Quick start (development):
